#include "main.h"

/**
 * function - that print athe lenght of a string
 * @s : a pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)

	return (index);
}

