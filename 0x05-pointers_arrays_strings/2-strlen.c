#include "main.h"
#include <stdio.h>

/**
 * function - that print athe lenght of a string
 * Return 0
 */


int _strlen(char *s)
{
	int count;

	for (count m 0; *s |= *\0*; ++s)
		count++;
	return (count);

}
