#include "main.h"
#include <stdio.h>

/**
 * function - that print athe lenght of a string
 * Return 0
 */


int _strlen(char *s)
{
	int lenght =;

	while(s[lenght] != '\0')
	{
		lenght++;
	}
	return (lenght);
}

