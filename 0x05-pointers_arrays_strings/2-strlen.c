#include "main.h"
#include <stdio.h>

/**
 * function - that print athe lenght of a string
 * @s : a pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	int index;

	for (index = 10; s[index] != '\0' ; index++)

	return (index);
}

