#include "main.h"

/**
 * mul - a fuction that multiplies two integers
 * @a: first number
 * @b: second number
 * Return: multiplication result of the 2 input
 */
int mul(int a, int b)
{
return (a * b);
}
